@model IEnumerable<ImDone.Tanda>

@{
    ViewBag.Title = "Index";
}

<h2>Tandas</h2>

<form method="get" style="float:right; position:absolute; left:1088px; top:150px">
    <input type="text" id="Criterio" name="Criterio" />
    <input type="submit" id="cmdBuscar" value="Buscar" />
</form>

<br />

<p>
    @Html.ActionLink("Exportar Excel", "exportaExcel", "Tandas", null, new { @class = "btn btn-danger" })



    @Html.ActionLink("Crear nuevo", "Create", "Tandas", null, new { @class = "btn btn-primary" })

</p>

<table class="table">
    <tr>
        <th>
            Tanda
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.periodo_tanda)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.id_tanda }) |
                @Html.ActionLink("Details", "Details", new { id = item.id_tanda }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.id_tanda })
            </td>
        </tr>
    }

</table>
